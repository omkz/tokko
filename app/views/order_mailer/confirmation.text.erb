Order Confirmed #<%= @order.id %>
===============================

Hi <%= @order.customer_name %>,

Thank you for your order! Here's a summary:

ITEMS
-----
<% @items.each do |item| %>
- <%= item.product_variant.product.name %> (<%= item.product_variant.option_text %>) x<%= item.quantity %> — Rp <%= number_with_delimiter(item.unit_price.to_i * item.quantity) %>
<% end %>

TOTAL: Rp <%= number_with_delimiter(@order.total_price.to_i) %>

SHIPPING TO
-----------
<%= @order.customer_name %>
<%= @order.shipping_address %>

If you have questions, reply to this email.

— Tokko
